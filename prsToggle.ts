import {SharingCamelConfig, Link, LinkStatement, ParseOptions, MathOp, AllProps, AP, IObserve, Share, Source} from './types';
import {RegExpOrRegExpExt} from 'be-enhanced/types';

let reToggleStatements: RegExpOrRegExpExt<PTS>[] | undefined;
export async function prsToggle(scc: SharingCamelConfig, links: Link[], pp: any){
    const {} = scc
}

interface ToggleStatement{

} 

type PTS = Partial<ToggleStatement>;